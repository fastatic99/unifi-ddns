name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy
    steps:
      - uses: actions/checkout@v3
      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{BwKtVCH25EpeQ3ERln6D6VFcoYUeNqYLAEAr2kxu}}
          accountId: ${{3b4233703450de486f0077eb4ad8094f }}
